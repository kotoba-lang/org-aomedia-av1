;; kbb reads this file. It does NOT read deps.edn, and the
;; .cljk here are run as bare `kbb -M:test`, so a coordinate declared only
;; there is invisible to the engine.
;;
;; Unblock recipe (runbook: kotoba-runner-loading.md): copy the coords from
;; deps.edn HERE — every sha below is COPIED from deps.edn, not chosen.
;; 2026-09-21 conformance cron attempt (first).
{:paths ["src" "."]
 :deps {
        ;; kotoba.lang.error replaced clojure.lang.ExceptionInfo here (2026-09-25).
        ;; An explicit floor: tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/error {:git/sha "838dec56c9a5b74f9eec825a2fb28bda157f8cd3"}
        ;; kotoba.test (clojure.test-compatible surface), not clojure.test --
        ;; scripts/migrate-clojure-test-to-kotoba-test.cljk. Explicit floor.
        io.github.kotoba-lang/test {:git/url "https://github.com/kotoba-lang/test.git"
                                    :git/sha "2ab4885f99fd25552d380d0f6b0b54c5a72eb7ae"}
        io.github.kotoba-lang/codec-primitives
        {:git/url "https://github.com/kotoba-lang/codec-primitives.git"
         :git/sha "ffa076057caedcfaabb9645c84e002ee468f8268"}}}
